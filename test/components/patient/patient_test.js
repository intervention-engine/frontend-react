import { renderComponent , expect } from '../../test_helper';
import Patient from '../../../src/containers/Patient';

describe('Patient', () => {
	// let component;
	// beforeEach(() => {
	// 	component = renderComponent(Patient);
	// });
	// it('has the correct class', () => {
	// 	expect(component).to.have.class('patient');
	// });
});
